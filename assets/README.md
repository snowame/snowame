# assets

`Shiyin.jpg` 是首页的整页固定背景。换图只需用同名文件覆盖它（`Shiyin.jpg`），或改 `index.html` 里 `.bg` 的 `url("assets/Shiyin.jpg")`。

- 推荐：高调、明亮的竖构图，主体大致居中偏上（首屏标题压在底部，会露出人物上半身）
- 背景上覆了一层暖白柔纱 + 文字磨砂光晕；若换成偏暗的图，需调高 `.veil` 透明度才能保证文字清晰

> 版权：eden* / 诗音的图像版权归 minori 所有，仅作个人粉丝页面非商用展示。
